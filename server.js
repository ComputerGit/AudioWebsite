// require("dotenv").config(); // Ensure parentheses are included to call the function

// const bodyParser = require("body-parser");
// const express = require("express");
// const mongoose = require("mongoose");
// const cors = require("cors");

// const app = express();
// const port = process.env.PORT || 5000;
// const MONGO_URI = process.env.MONGO_URI;

// console.log("MONGO_URI:", MONGO_URI); // Log the MONGO_URI to ensure it is being read correctly

// app.use(cors()); // Correctly use the CORS middleware
// app.use(bodyParser.json()); // Parse JSON bodies

// mongoose
//   .connect(MONGO_URI)
//   .then(() => console.log("MongoDB Connected"))
//   .catch((err) => console.log(err));

// // Add a basic route to test the server
// app.get("/", (req, res) => res.send("API Running"));

// app.listen(port, () => console.log(`Server started on port ${port}`));
